debug: false
server:
  context-path: /admin
  port: 8099
  tomcat:
    access_log_enabled: true

spring:
  redis:
    database: 1
    host: 10.11.8.6
    port: 6379
    password:
    pool.max-active: 8
    pool.max-wait: -1
    pool.max-idle: 8
    pool.min-idle: 30
    timeout: 0
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://10.11.8.8:3308/ctdn?characterEncoding=utf-8
    username: ctdn
    password: DArYXXHG4M#kqQY
    driver-class-name: com.mysql.jdbc.Driver
    minIdle: 1
    maxActive: 10
    initialSize: 1
    timeBetweenEvictionRunsMillis: 3000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
  jpa:
    show-sql: true
    properties:
      hibernate:
        hbm2ddl:
          auto: none

  thymeleaf:
    cache: false
    cache-period: 0
  template:
    cache: false
